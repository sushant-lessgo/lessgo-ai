# cold-outreach — implementation audit

## Phase 1 — Data layer: Prisma models + migration + credit constants + model key

### Files changed
- `prisma/schema.prisma` — added 3 models + 3 back-relations on `Project`.
- `prisma/migrations/20260710202106_cold_outreach/migration.sql` — generated by `migrate dev`.
- `src/lib/creditSystem.ts` — added `CREDIT_COSTS.OUTREACH_SCRAPE` + 2 `UsageEventType` members.
- `src/lib/modelConfig.ts` — added `'cold-outreach'` endpoint to the union + both model tiers.

### What changed
- **schema.prisma**: added `OutreachIntake` (single-row, `projectId @unique`), `ProspectScrape`
  (`@@unique([projectId, urlKey])`, `[tokenId]` index), `OutreachMessage` (many-row library,
  `[userId, createdAt]` + `[tokenId, createdAt]` indexes) — all with bare `userId String`,
  `tokenId`, `project @relation(..., onDelete: Cascade)`, mirroring `SocialPost`/`EmailSequence`.
  Added back-relations `outreachIntake OutreachIntake?`, `prospectScrapes ProspectScrape[]`,
  `outreachMessages OutreachMessage[]` on `Project`. Field lists / indexes / constraints match the
  plan and the generated SQL exactly.
- **creditSystem.ts**: `CREDIT_COSTS.OUTREACH_SCRAPE = 1`; `UsageEventType.OUTREACH_SCRAPE =
  'outreach_scrape'` and `OUTREACH_GENERATION = 'outreach_generation'` (generation credits-free;
  ledger row is gating source-of-truth, per sibling precedent).
- **modelConfig.ts**: `'cold-outreach'` added to `Endpoint`; `{ primary: GPT_4O_MINI, backup: null }`
  entry added in BOTH `cheap` and `production` tiers (OpenAI-only path, email precedent).

### Migration
- Name/timestamp: `20260710202106_cold_outreach`.
- `npx prisma migrate dev --name cold_outreach`: migration created **and applied cleanly** — Neon dev
  DB "now in sync with your schema". SQL reviewed, matches the plan (see migration.sql).

### Verification
- **Migration apply**: clean (applied to Neon dev DB).
- **`npm run test:run`**: PASS — `Test Files 107 passed | 1 skipped`, `Tests 1810 passed | 3 skipped`,
  0 failures. No regressions.
- **`npx prisma generate`**: BLOCKED by environment — `EPERM: operation not permitted, rename
  query_engine-windows.dll.node`. The shared `node_modules/.prisma/client` query-engine DLL (dated
  Jul 6) is held open by the user's running **Prisma Studio** (pid 31520) and **Next dev servers**
  (pids 41476 main / 61684 email-sequences worktree). Retried 3×, still locked. Not killed (user
  processes). The migration itself succeeded; only client codegen is blocked.
- **`npx tsc --noEmit`**: fails ONLY on stale-client errors — the currently-generated client predates
  the stacked branches, so it lacks `emailSequence`/`socialPost` too (10 errors, all in existing
  `api/email-sequences/*` and `api/social/*` files). **Zero errors reference my Phase-1 changes**
  (`OutreachIntake`/`ProspectScrape`/`OutreachMessage`, `creditSystem`, `modelConfig`). Once the DLL
  lock clears and `prisma generate` runs, tsc will be clean.

### Deviations from plan
- None functional. Only environmental blocker: `prisma generate` + `tsc` cannot be confirmed green
  because the Windows query-engine DLL is locked by the user's running Prisma Studio / dev servers.
  **Action for user:** close Prisma Studio (and optionally dev servers), then run
  `npx prisma generate` — this regenerates the client for all stacked branches; `tsc` then clean.

### Open risks
- The shared Prisma client is stale relative to this stacked branch (pre-social-posts/email-sequences).
  A `prisma generate` (post-lock) is required before any runtime/tsc use of the new models — deferred
  to phases 2+ which actually consume them.
