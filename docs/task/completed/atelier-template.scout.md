# atelier-template — scout findings (for planner)

Consolidated read-only findings feeding the plan. Pointers only; verify at implement time.

## A. template-factory machinery (merged 1c6ca3ad — follow exactly, don't reinvent)

- **Conformance:** `templateConformance(id)` in `src/modules/templates/templateConformance.ts:233`; auto-fires via `conformance.test.ts` loop over `templateIds`. Checks: (a) engine-core sections resolve real (both modes); (b)/(b+) declared block-backed capability ⇒ `capabilitySections` entry resolves real, no orphans; (c) variant resolution + distinctness; block-manifest `minCards≤maxCards`, `consumes ⊆ contract`; (d) collection family (dormant); (e) variant-swap integrity. To pass: add `RESOLVERS` entry (`:79`, `{resolve, placeholder}`), `templateMeta` entry, `blockManifests[id]`. Global parity test (`conformance.test.ts:84`) requires `RESOLVERS`/`templateMeta`/`templateIds` identical key sets.
- **Opt-in (enroll explicitly in conformance.test.ts):** `assertEditorBasics(id)` (:494 — logo/text/image-slots/buttons/nav/footer/collection add-remove-reorder/social/form/knob switching), `assertKnobConformance(id, knobs)` (:605), `assertLooksConformance(...)` (:669). Precedent = hearth+meridian (conformance.test.ts:98-166).
- **templateMeta** (`src/modules/templates/templateMeta.ts:56`, pure data, MUST NOT import a template module): `copyEngines: CopyEngine[]`, `capabilities: CapabilityId[]`, `designStyles: DesignStyle[]`, opt `capabilitySections` (cap→sectionType), opt `looks: TemplateLook[]` (the "List-3"), opt `retired`/`bespoke`. `TemplateLook` = `{id,label,blurb,knobs,variantId,paletteId}` (refs must be real — looks-truthfulness gate). Vocab CLOSED (`@/types/brief`).
- **Knobs** (`src/modules/templates/knobs.ts`): 5 STANDARD axes — `buttonShape`, `cardStyle`, `density`, `typePairing`, `texture`. If a template declares `knobs` it MUST declare all 5; each value ⊆ standard vocab + include axis default (default emits NO attr/CSS). Render via shared `src/modules/templates/shared/knobCss.ts` (`serializeKnobOverrides`, `knobDataAttributes`) inside SSRTokens + ThemeInjector. Per-axis CSS values in template `tokens.ts`. Blocks NEVER branch on a knob. Precedent = hearth. **Vestria ships NO knobs — atelier may skip knobs entirely (simpler) unless spec's btn-radius/density/card-treatment axes are required.**
- **Handoff lint:** `npm run kit:lint <file.html>` (`src/modules/templates/handoffLint.ts`, `scripts/lintHandoff.ts`). Regex over HTML string. Checks: all engine core sections via `data-section`, every required slot via `data-slot`/`data-element-key`, axes structured (`:root`+`[data-palette]`+`[data-variant]`+≥1 `data-knob-*`), fonts ⊆ `SELF_HOSTED_FONTS`, self-contained (no external urls). Run BEFORE port.
- **Kit generator:** `npm run kit:generate` (`src/modules/engines/designKit.ts`). Per-engine markdown brief (sections order, per-section slots, card min/max, flagship manifest, 5 knob axes, format constraints incl `classPrefix: lg-<templateId>-`). Engine→flagship: thing→meridian, trust→hearth, work→granth. `SELF_HOSTED_FONTS` whitelist at designKit.ts:80.
- **Screenshot parity:** `npm run test:parity` (`e2e/parity.spec.ts`). Renders `/dev/blocks/<template>` (`TemplateBlocksStage.tsx`), stacks edit+published bands, pixel-diffs them to EACH OTHER, `PASS_THRESHOLD=0.03`. `TEMPLATES=['meridian','hearth']` (:18) — **add 'atelier' + blockMocks entries**. `?parityBreak=1` negative control must exceed threshold. Content parity = separate jsdom `renderParity.<t>.test.tsx`.
- **Skill:** `.claude/skills/new-template/SKILL.md` = canonical procedure. Landmines: dual-renderer pairs, published/client boundary (never import named value from `'use client'` into `.published`), per-block CSS in plain `styles.ts`, prefix classes `lg-<id>-`, margin-collapse via data-surface (pad inside), self-hosted fonts only, ONE behaviors asset, two-identifier (lowercase `type` + PascalCase `LayoutName`).

## B. Template anatomy — CLONE SOURCE = vestria (`src/modules/templates/vestria/`)

- **TemplateModule contract** (`src/types/template.ts:48-104`): `resolveBlock(blockType, mode, layoutName?)`, `ThemeInjector` (client), `SSRTokens` (server), `getSurfaceForSection`, `defaultPaletteId`, `variants[]`+`defaultVariantId`, opt `paletteImageKeywords`, opt `knobs`.
- **Registry** (`src/modules/templates/registry.ts:14`): add async loader block mapping template exports → generic contract. Also add `'atelier'` to `TemplateId` union in `src/types/service.ts` + confirm `usesTemplateModule()` gate. **Zero edits to `componentRegistry{,.published}.ts`** — they dispatch purely via `getLoadedTemplate(id).resolveBlock`.
- **vestria files:** `index.ts` (server-safe barrel = TemplateModule surface; generic `resolveBlock` wrapper; NEVER re-export client helpers), `tokens.ts` (base CSS-vars + variants folded in + `serializeBaseTokens/VariantOverrides`; vestria also has a **mood axis** — DROP for atelier), `palettes.ts` (accent overrides under `[data-palette]` + `defaultVestriaPalette` + `serializePaletteOverrides`), `sectionRules.ts` (`getSurfaceForSection`), `ThemeInjector.tsx` (client), `resolveVestriaBlock.ts` (section-type→{edit,published} map + placeholder), `components/VestriaSSRTokens.tsx`, `components/VestriaEditable.tsx`, `hooks/useVestriaBlock.ts`, `paletteSelection.ts`, `imageKeywords.ts`, `VestriaPlaceholderBlock.tsx`, `coreParity.test.ts`, `registration.test.ts`, `README.md`.
- **Blocks = `.core.tsx` single-source triad** (vestria + granth only): `<Block>.core.tsx` (ALL markup, pure, renders via injected primitives `E`), `<Block>.tsx` (`'use client'` edit wrapper wiring `useVestriaBlock`+`editPrimitives`), `<Block>.published.tsx` (server-safe, `makePublishedPrimitives`, flat props), `styles.ts` (per-block CSS string). Shared: `blocks/{primitives.ts,editPrimitives.tsx,publishedPrimitives.tsx,shared/styles.ts}`. `coreParity.test.ts` enforces core purity.
- **Prefixing:** every symbol/file prefixed `Atelier*`/`atelier*`; `<style>` id prefixed (`atelier-theme`); but **CSS vars are generic/shared** (`--paper`,`--accent`,`--ink`) so `data-surface`/`data-palette` stay template-agnostic. Class names prefix `lg-atelier-` (kit format constraint) — note vestria block classes; match kit's classPrefix.
- **Axes layering:** `data-palette` (accent) → `data-variant` (typefaces) → `data-surface` (per-section band consuming the vars). Renderer wraps each section in `<div data-surface>`; blocks must NOT paint own full-bleed bg or use outer `margin:N 0`.

## C. Multipage (vestria machinery — nav/sitemap/fan-out all free)

- **Page archetypes** = per-template PAGE contract in `src/modules/audience/product/pageArchetypes.ts`. `PageArchetypeDef` = `{key,title,pathSlug,required,defaultIncluded,allowedSections,requiredSections,defaultSections,description}`. Add `ATELIER_PAGE_ARCHETYPES` (5: Home/Work/Experiences/About/Contact) + register in `PAGE_ARCHETYPES_BY_TEMPLATE`. `getPageArchetypesForTemplate()` returns menu ONLY if template declares `multipage` in templateMeta. `isMultipage()` gates on capability + Brief structure.mode.
- **Nav** derived (not template-declared): `src/utils/pageLinks.ts` (`buildPageLinkOptions`, `deriveNavLinks`) from project pages; template NavHeader block only seeds. **SEO sitemap.xml** separate (`src/lib/seo/buildSitemapXml.ts`).
- **Fan-out:** strategy route proposes sitemap (LLM constrained by archetype menu) → agreed sitemap persisted → `src/modules/generation/multiPageAssembly.ts` (`buildMultiPageSkeleton`, `mergePageIntoFinalContent`, `finalizeMultiPageGeneration`). Home carries header/footer chrome; subpages body-only. **But work-engine COPY GEN is OUT of scope (manual-fill)** — atelier ships the page grammar + blocks; copy is hand-authored.
- **Gate 0 status:** `docs/reports/qa-vestria.md` (prod run 2026-07-07) — multipage machinery PASSED (onboarding, 3-page gen, resolution, editor+published render, multipage publish, per-host sitemap, form, SiteContext fan-out). BUT 7 editor punch-list findings open (rich-text toolbar broken on Vestria fields, editor/published serif mismatch, section Delete freezes, undo/redo disabled, section-regen fails). Spec says absorb into atelier's parity gate if open → **FLAG: these editor defects may recur on atelier; parity/editor-basics phase must re-verify.**

## D. Serve gate + capabilities (the photographer flip)

- **fit()** (`src/modules/templates/fit.ts:40`): rejects retired/bespoke; requires engine ∈ `meta.copyEngines`; `required.every(cap => meta.capabilities.includes(cap) || sharedBlockCapabilities.includes(cap) || PLATFORM_CAPABILITIES.includes(cap))`.
- **decideServe()** (`src/modules/brief/serveGate.ts:145`): SERVE iff businessType known + in-ICP + resolvedEngine∈{thing,trust,work} + non-empty `shortlist()`. Photographer's `rungC` gallery probe runs `fit(t, 'work', [...required,'gallery'])`; today no template supplies gallery → MANUAL. **Flip = a non-bespoke work-engine template DECLARING `gallery` in templateMeta.capabilities + a `capabilitySections:{gallery:<sectionType>}` with a real resolving block pair.** Then shortlist non-empty → serve; work→audienceType `writer` (`BRIDGEABLE_ENGINES['work']`). Authoritative at `src/app/api/brief/confirm/route.ts:71`.
- **businessType photographer** (`src/modules/businessTypes/config.ts:242`): copyEngine `work`, requiredCapabilities `['gallery']`, defaultStyle `editorial-craft`, extractionSchemaKey `work`, likelyIntents enquiry/book-call/follow-social, structureDefault `single`. **GOTCHA: photographer structureDefault=`single` but spec wants 5-page — check if serve/onboarding forces multi, or businessType needs `multi`.**
- **Capability vocab** (closed, `src/types/brief.ts:24-58`): includes `multipage, gallery, packages, lead-form, bilingual, ...`. Three satisfaction lanes: template-own (templateMeta), shared-block (`src/modules/generatedLanding/sharedBlocks/capabilities.ts` — e.g. lead-form), platform (`PLATFORM_CAPABILITIES=['bilingual']`). `STRUCTURAL_CAPABILITIES=['multipage','bilingual']` exempt from block-evidence. **`gallery` + `packages` are block-backed** → atelier MUST declare + provide capabilitySections + real block pairs.
- **Honesty:** block-backed caps auto-checked by `templateConformance` group (b) — no separate honesty test needed (just enroll `templateConformance('atelier')`). Structural caps use `i18nHonesty.test.ts` model. **D5 note (`i18nHonesty.test.ts:16`): per-template Dutch/bilingual typography fidelity is a MANUAL gate "first exercised in the atelier build."**
- **designStyle gotcha:** `editorial-craft` claimed by vestria(thing)+lumen(work,bespoke). Atelier `designStyles` + `copyEngines:['work']` must yield a non-bespoke work template whose style resolves for the photographer pick. Confirm DesignStyle vocab + choose warm/editorial value(s) matching spec (warm/human demand sweet spot).

## E. i18n / bilingual — INHERITED FREE (i18n-phase-1 merged)

- `bilingual` is a **PLATFORM capability** (`fit.ts` `PLATFORM_CAPABILITIES`), satisfied for every non-retired template; `requiredCapabilitiesFromBrief()` adds it when `brief.locales.length>1`. **Atelier need NOT list bilingual in templateMeta.**
- Mechanism: base flat `content` = default locale + text-only overlay `localeContent:{[locale]:{[sectionId]:{[elementKey]:string|string[]}}}` + `localeConfig:{locales,defaultLocale}` (`src/types/core/content.ts`). Resolver `src/lib/i18n/localeContent.ts`. **DO NOT copy lumen's `_nl` twin-field / `data-en`/`data-nl` approach.**
- Editor toggle: `LanguageToggle.tsx`+`LocaleSettings.tsx` in EditHeader; sets `activeLocale`; blocks thread via `useTemplateBlock`. Published: platform emits one static doc per locale at `/{loc}` + hreflang + `switcher.v1.js` (geo/localStorage/navigator boot). **All shared/platform — atelier's only requirement: blocks read text via the standard block funnel + `.published.tsx` renders plain resolved text.** Free if built on the standard pattern.
- **v1 gaps (flag):** nav labels, per-collection-item card text, form field labels, media are locale-SHARED (not translated). Kundius NL site must live with this or hand-fill base.

## F. Fonts + hero-slider behaviors

- **Fonts:** `src/styles/fonts-self-hosted.css` (@font-face per family) + `public/fonts/<slug>/*.woff2`. **Hanken Grotesk ALREADY present** (added for Surge) — reuse. **Only Bricolage Grotesque net-new** (variable opsz+wght → ship `woff2-variations` weight range like Archivo/Space Grotesk block). CSS copied to `public/assets/` by buildAssets → published HTML links it → **rebuild mandatory**.
- **Preload:** add `case 'atelier':` in `src/modules/templates/CriticalFontPreload.tsx` (`criticalFontHrefs`) returning hero display font woff2 (variant-branch if display face swaps).
- **Slider behaviors (4 files together):** (1) new `src/lib/staticExport/atelierSliderBehaviors.js` — vanilla IIFE, idempotent boot guard, no-op if markup absent (mirror `lumenBehaviors.js`); (2) `scripts/buildAssets.js:51` add `{src:'atelierSliderBehaviors.js', out:'slider.v1.js'}`; (3) `src/lib/staticExport/htmlGenerator.ts` add `usesAtelier` flag (~:145) + `<script src=".../assets/slider.v1.js" defer>` (~:388) gated; (4) paired blocks share a data-attribute contract with the JS. **Immutable filename contract** — first release `slider.v1.js` fine; never overwrite v1 bytes (ship v2 if behavior changes).
- **Published block:** static markup w/ data hooks, **first image visible by default (no-JS static fallback)**; asset adds crossfade/arrows/dots on boot. **Editor block:** React hooks version (useState/useEffect), identical layout/CSS. No reusable carousel exists — clone the split from TechPremium Gallery (`.tsx` React vs `.published.tsx`+`tp` asset) or Lumen `LumenCategoryGallery`. `EditableImageCollection.tsx` = edit-side image-array primitive to reuse.
- **Build:** `npm run build` = build:published-css → build:assets → next build. New woff2/@font-face + slider.v1.js reach published pages only AFTER rebuild.

## G. ⚠️ Two cross-cutting decisions for the planner

1. **Work-engine core sections are writer-shaped.** `engineCoreSections.work = ['hero','about','books','writing','praise','footer']` (`src/modules/engines/coreSections.ts`; granth's canonical 6 — books/writing/praise are writer-specific). Code comment: "Revisit work-core when a second shortlist-eligible work template lands." **Atelier IS that template.** Conformance group (a) (`conformance.test.ts:192`) requires atelier resolve ALL of `engineCoreSections.work` as real blocks. Scope item #4 wants the work grammar formalized to `cover/work/packages/about/quote-band/contact`. **DECISION: generalize `engineCoreSections.work` to a photographer-servable set + keep granth passing (granth blast-radius: granth must still resolve the new core), OR map atelier's sections onto the writer-shaped names.** This is a foundational, early-phase decision with granth regression risk. `designKit(work)` + handoff lint derive from these core sections too.
2. **Designer HTML MISSING.** `template-design/` has NO `KPDirection2Atelier.html` / `KPDirection3Kontur.html` / `atelierKonturBrief.md` (only TechPremium/Vestria/Granth/Patrika/Nishant HTML + designer-workspace/ + MAPPING.md). Spec dep #3: blocks the PORT phase only. **DECISION: sequence all foundation phases (TemplateId+registry scaffold with placeholder blocks, templateMeta, page archetypes, work-grammar generalization, capability decls, serve-gate flip, fonts, conformance enrollment) BEFORE the visual block port. Mark the block-port phase a HUMAN GATE requiring approved HTML + founder taste-picks (Step-1 tile pick + palette pass are in-motion human gates per spec).** Kundius live-site + parity QA sign-off also human-gated at the end.
